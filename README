Auto-posting to LinkedIn page
1. Set up a virtual environment:
    python3 -m venv venv_name
2. Install the dependencies:
    pip install -r requirements.txt
3. Make env.py and add all required credentials and inputs into it
4. Run script with command:
    python3 apiPosting.py [ru]
('ru' param means posting in russian, by default - in English)